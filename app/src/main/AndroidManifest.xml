<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.soldaplication">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_sold"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_sold"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".Activities.LoadingActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".Activities.LoginActivity" />
        <activity
            android:name=".Activities.MainActivity"
            android:label="@string/title_activity_main" />
        <activity android:name=".Activities.RegisterStepOneActivity"/>
        <activity android:name=".Activities.RegisterStepTwoActivity"/>
    </application>

</manifest>